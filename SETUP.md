# Flow3 + Flow3_Queue — คู่มือติดตั้ง

ระบบแนบเอกสาร กยศ. อัตโนมัติ ทำงานเป็นคิว วนทีละชุดจนหมด

| ไฟล์ | คืออะไร |
|---|---|
| `RunOneRound.txt` | ตัวทำงาน 1 ชุดต่อ 1 รอบ (แก้ไข/แพตช์ครบแล้ว พร้อม paste) |
| `Flow3_Queue.txt` | ตัวจัดคิว วนเรียก `RunOneRound` ซ้ำ ๆ |
| `PlayErrorAlarm.txt` | Subflow เสียงเตือน |

---

## 1. โครงสร้างโฟลเดอร์

ทั้งหมดอยู่ใต้ `D:\SPU_JOB\RPA\Flow3\`

```
D:\SPU_JOB\RPA\Flow3\
│
├─ Queue\                     [ต้องสร้างเอง]  ── ที่วางงานรอคิว
│  ├─ 123-1.xlsx                              ไฟล์ Excel ของชุด
│  ├─ 123-1\                                  โฟลเดอร์ PDF ชื่อเดียวกัน
│  │  ├─ 1234567890123.pdf
│  │  └─ 9876543210987.pdf
│  ├─ 124-1.xlsx
│  ├─ 124-1\
│  │  └─ ...
│  │
│  ├─ _Working\               [สร้างอัตโนมัติ] ── งานที่กำลังทำ ต้องว่างเสมอ
│  └─ Archive\                [สร้างอัตโนมัติ] ── งานที่เสร็จแล้ว
│     └─ 20260912_143022_123-1\
│        ├─ 123-1.xlsx
│        └─ 123-1\
│
├─ Input\                     [ต้องสร้างเอง]  ── ต้องว่าง! ตัวจัดคิวเอา Excel มาวางเอง
│  └─ Archive\                [ต้องสร้างเอง]  ── Excel ที่ทำเสร็จแล้ว
│
├─ ProcDocs\                  [ต้องสร้างเอง]
│  └─ 2569\                   [ต้องสร้างเอง]  ── ปี พ.ศ. 4 หลัก มีได้ "1 โฟลเดอร์เท่านั้น"
│     └─ Term_1\              [ต้องสร้างเอง]  ── มีได้ "1 โฟลเดอร์เท่านั้น" (Term_1/Term_2/Term_S)
│        └─ (ว่าง)                             ตัวจัดคิวเอาโฟลเดอร์ PDF มาวางเอง
│
├─ Processing\                [ต้องสร้างเอง]  ── ที่พักไฟล์ Excel ระหว่างอ่าน ต้องว่าง
├─ Output\                    [ต้องสร้างเอง]  ── Excel ที่ประมวลผลแล้ว (สะสมไว้)
│
├─ Log\                       [ต้องสร้างเอง]  ── log รอบปัจจุบัน ต้องว่างตอนเริ่ม
├─ Output_Log\                [ต้องสร้างเอง]  ── log เก่าที่เก็บแล้ว (สะสมไว้)
├─ Log_Queue\                 [สร้างอัตโนมัติ] ── queue.log ของตัวจัดคิว
│
├─ State\                     [ต้องสร้างเอง]  ── โฟลเดอร์ต้องมี ไฟล์ข้างในสร้างเอง
│  ├─ resume_success.txt                      กันแนบซ้ำ — ห้ามลบ!
│  ├─ print_success.txt                       กันพิมพ์ใบนำส่งซ้ำ — ห้ามลบ!
│  ├─ STOP.txt                                สร้างเมื่อต้องการหยุด Flow3 รอบปัจจุบัน
│  └─ QUEUE_STOP.txt                          สร้างเมื่อต้องการหยุดลูปคิว
│
└─ Sound\                     [ต้องสร้างเอง]
   └─ error_alarm.wav         [ต้องหาไฟล์มาใส่] ── เสียงเตือนเวลาระบบหยุด
```

### คำสั่งสร้างทีเดียวจบ

เปิด PowerShell แล้ววาง:

```powershell
$r = 'D:\SPU_JOB\RPA\Flow3'
@(
  'Queue','Input\Archive','ProcDocs\2569\Term_1','Processing',
  'Output','Log','Output_Log','State','Sound'
) | ForEach-Object { New-Item -ItemType Directory -Force -Path "$r\$_" | Out-Null }
Write-Host "สร้างโฟลเดอร์เรียบร้อย"
```

> เปลี่ยน `2569` และ `Term_1` ให้ตรงกับปี/เทอมที่ใช้จริง

---

## 2. นำเข้า Flow ใน Power Automate Desktop

ทั้งหมดอยู่ใน **Flow เดียว** ชื่อ `Flow3` แบ่งเป็น subflow

| Subflow | เนื้อหา | หน้าที่ |
|---|---|---|
| `Main` | `CALL Flow3_Queue` บรรทัดเดียว | จุดเริ่ม |
| `Flow3_Queue` | `Flow3_Queue.txt` | ตัวจัดคิว วนลูป |
| `RunOneRound` | `RunOneRound.txt` | ตัวทำงาน 1 ชุด |
| `PlayErrorAlarm` | `PlayErrorAlarm.txt` | เสียงเตือน |

`RunOneRound.txt` แก้ไข/แพตช์ครบแล้ว (อ่านเลข KTB, วนรอแท็บ PDF, `EXIT`→`GOTO` ทั้ง 11 จุด) — paste ตรงได้เลย ไม่ต้องแก้อะไรเพิ่ม

### ขั้นตอน

1. เปิด Flow `Flow3` → กด `+ New` สร้าง subflow ชื่อ **`RunOneRound`** → paste จาก `RunOneRound.txt`
2. ไปแท็บ `Main` → `Ctrl+A` → `Delete` → ลาก action **Run subflow** ลงมา → เลือก `Flow3_Queue`
   (เหลือบรรทัดเดียวเท่านั้น)
3. สร้าง subflow **`Flow3_Queue`** → paste จาก `Flow3_Queue.txt`
4. สร้าง subflow **`PlayErrorAlarm`** → paste จาก `PlayErrorAlarm.txt`

---

## 3. ตรวจ `GOTO` / `Label` ใน `RunOneRound`

`RunOneRound.txt` แปลง `EXIT` (จบทั้ง Flow) เป็น `GOTO EndOfRound` ไว้ครบ 11 จุดแล้ว เพราะ `EXIT` ใน PAD จบทั้ง Flow ไม่ใช่แค่ subflow — ถ้าไม่แปลง ทำชุดแรกเสร็จแล้วลูปคิวจะตายทันที

หลัง paste ให้ดู `Errors list`: ถ้า action **Go to** / **Label** ไม่มีในรุ่น PAD ที่ใช้ จะขึ้น error ชี้ตำแหน่งให้ตรง — แก้โดยลาก **Flow control → Go to** / **Flow control → Label** แทนที่บรรทัดนั้น

---

## 4. สิ่งที่ต้องแก้หลัง import

### 4.1 รหัสผ่านอีเมล — ต้องกรอกใหม่ทุกเครื่อง

ค่า `Password: P'AQAAANCMnd8...'` ถูกเข้ารหัสผูกกับ **บัญชี Windows ของเครื่องที่สร้าง**
เครื่องอื่นถอดรหัสไม่ได้ → อีเมลจะส่งไม่ออก

ต้องเปิดทุก action `Send email` แล้วพิมพ์รหัสผ่านใหม่ด้วยมือ

| Flow | จำนวน action ที่ต้องแก้ |
|---|---|
| `RunOneRound` | 12 จุด |
| `Flow3_Queue` | 1 จุด |

### 4.2 ไดรฟ์ไม่ใช่ D:

แก้ที่ตัวแปรบนสุดของแต่ละ Flow เท่านั้น (path อื่นอ้างอิงตัวแปรทั้งหมด)

- `Flow3` — บรรทัด 1–10
- `Flow3_Queue` — บรรทัด 10–21
- `PlayErrorAlarm` — ในสคริปต์ PowerShell



### 4.3 บั๊กใน `PlayErrorAlarm` — path เสียงไม่ตรงกัน

```
บรรทัด 1 :  Sound\error_alarm.wav          ← น่าจะถูก
PowerShell:  Sound\Sounderror_alarm.wav     ← เกินคำว่า Sound
```

แก้ให้เหมือนกันทั้งสองที่ ไม่งั้น PowerShell จะ `throw "ไม่พบไฟล์เสียง"`

---

## 5. วิธีใช้งานประจำวัน

### เตรียมงานเข้าคิว

1. **ปิด Excel ให้หมดก่อน** (ไฟล์ล็อก `~$xxx.xlsx` ทำระบบหยุด)
2. วางลงใน `Queue\` เป็นคู่ ชื่อต้องตรงกัน:
   ```
   Queue\123-1.xlsx      ← ไฟล์ Excel
   Queue\123-1\          ← โฟลเดอร์ PDF (ชื่อเดียวกัน ไม่มีนามสกุล)
   ```
3. PDF ต้องไม่เกิน **50 ไฟล์ต่อชุด** เกินให้แยกเป็น `123-1(2)`, `123-1(3)`
4. ชื่อไฟล์ PDF ต้องมีเลขบัตรประชาชน 13 หลัก

### สั่งรัน

กดปุ่ม **Run** ตามปกติ — `Main` จะเรียก `Flow3_Queue` ให้เอง

ระบบจะวนทำทีละชุดตามลำดับชื่อ จนคิวหมดแล้วหยุดเอง

### สั่งหยุด

| ต้องการ | สร้างไฟล์ |
|---|---|
| หยุดลูปคิว (ทำชุดปัจจุบันให้จบก่อน) | `State\QUEUE_STOP.txt` |
| หยุด Flow3 รอบปัจจุบันกลางคัน | `State\STOP.txt` |
| หยุดทั้งหมด | สร้างทั้งสองไฟล์ |
| หยุดเสียงเตือนที่ดังค้าง | `State\QUEUE_STOP.txt` |

ระบบลบไฟล์ธงให้เองหลังหยุด ไม่ต้องลบเอง

---

## 6. เมื่อระบบหยุดกลางคัน

ระบบจะ **ส่งอีเมล + เด้งหน้าต่าง + เล่นเสียงเตือนทุก 20 วินาทีไม่หยุด** จนกว่าจะมีคนมาจัดการ

**ขั้นตอนแก้:**

1. สร้าง `State\QUEUE_STOP.txt` เพื่อหยุดเสียง
2. เปิด `Log_Queue\queue.log` อ่านบรรทัด `[QUEUE_HALT]` ดูสาเหตุ
3. งานที่ค้างอยู่ใน `Queue\_Working\`
4. แก้ปัญหาตามสาเหตุ แล้วย้ายงานจาก `_Working\` กลับไปที่ `Queue\`
5. เคลียร์ `Input\` ให้ว่าง
6. รัน `Flow3_Queue` ใหม่

> **ห้ามลบ `State\resume_success.txt`** — ไฟล์นี้จำว่าแนบอะไรไปแล้ว
> ลบแล้วรันซ้ำ = **แนบเอกสารซ้ำเข้าระบบจริง**

---

## 7. สาเหตุที่ระบบหยุดบ่อย

| ข้อความใน log | สาเหตุ | วิธีแก้ |
|---|---|---|
| `Input ยังมีไฟล์ Excel ค้างอยู่` | รอบก่อนมีรายการล้มเหลว หรือมีไฟล์ล็อก `~$` | ดู `Output_Log\attach_failed_*.log` แก้แล้วเคลียร์ `Input\` |
| `ไม่พบโฟลเดอร์ PDF ... คู่กัน` | วางแต่ Excel ลืมโฟลเดอร์ PDF หรือชื่อไม่ตรง | วางให้ครบคู่ ชื่อตรงกันเป๊ะ |
| `ไม่มีไฟล์ PDF อยู่เลย` | โฟลเดอร์ว่าง | ใส่ PDF |
| `เกินขีดจำกัด 50 ไฟล์` | ชุดใหญ่เกิน | แยกเป็น `(2)` `(3)` |
| `ProcDocs ต้องมีโฟลเดอร์ปีเพียง 1` | มีหลายปีปนกัน | เหลือไว้ปีเดียว |
| `Term_* เพียง 1 โฟลเดอร์` | มีหลายเทอมปนกัน | เหลือไว้เทอมเดียว |
| `พบงานค้างใน Queue\_Working\` | รอบก่อนแครช/ไฟดับ | ย้ายกลับไป `Queue\` แล้วเคลียร์ `_Working\` |
| `RunOneRound ทำงานผิดพลาด` | subflow ชื่อไม่ตรง หรือ action ข้างในพัง | ตรวจชื่อ subflow ว่าเป็น `RunOneRound` |
| ทำชุดแรกเสร็จแล้วหยุด ไม่วนต่อ | ยังไม่ได้แปลง `EXIT` เป็น `GOTO` | ทำตามหัวข้อ 3 |

---

## 8. โฟลเดอร์ที่โตขึ้นเรื่อย ๆ

ควรล้างเป็นระยะ (เช่น ทุกสิ้นเทอม):

| โฟลเดอร์ | ล้างได้ไหม |
|---|---|
| `Queue\Archive\` | ได้ — เก็บย้อนหลังตามนโยบาย |
| `Output\` | ได้ |
| `Output_Log\` | ได้ |
| `Input\Archive\` | ได้ |
| `Log_Queue\queue.log` | ได้ |
| `State\*.txt` | **ห้ามลบ** ระบบกันแนบซ้ำพังทันที |

---

## 9. ก่อนรันจริงครั้งแรก

- [ ] สำรอง `ProcDocs\` และ `State\` ไว้ก่อน
- [ ] ทดสอบด้วย 2 ชุดเล็ก (ชุดละ 1–2 PDF)
- [ ] ตรวจ `Log_Queue\queue.log` ว่ามี `[ITEM_DONE]` ครบทั้ง 2 ชุด
- [ ] ตรวจว่า `Queue\Archive\` มีงานทั้ง 2 ชุด และ `_Working\` ว่าง
- [ ] ตรวจว่าอีเมลสรุปส่งออกจริง

> `Flow3_Queue` **ลบโฟลเดอร์ทุกอันใน `ProcDocs\<ปี>\<เทอม>\` ก่อนวางงานใหม่ทุกรอบ**
> อย่าเก็บอะไรที่สำคัญไว้ในนั้น
