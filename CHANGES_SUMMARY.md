# สรุปการปรับปรุง Flow3 (ระบบแนบเอกสาร กยศ.)

เทียบกับโค้ดเดิม `MianFLow3Old.txt`

---

## สรุป 30 วินาที

| หัวข้อ | ก่อน | หลัง |
|---|---|---|
| จำนวนชุดต่อการกด Run 1 ครั้ง | 1 ชุด | ทำต่อเนื่องจนคิวหมด |
| เจอ error ในชุดใดชุดหนึ่ง | ปิดทั้งโปรแกรม | จบเฉพาะชุดนั้น แล้วระบบคิวตัดสินใจต่อ |
| PDF ใบนำส่งขึ้นช้า | ล้มเหลวหลังรอ 5 วินาที | รอได้สูงสุดประมาณ 80 วินาที |
| เลข KTB ของใบนำส่ง | ไม่ได้เก็บ | บันทึกลง `KTB\KTB.log` อัตโนมัติ |
| หยุดกลางคันแล้วรันต่อ | ต้องปิดโปรแกรมและย้ายไฟล์เอง | กด Run ใหม่ได้ทันที |
| เสียงเตือน | path ไฟล์เสียงผิด | ดังซ้ำจนกว่าจะสั่งหยุด |

**ส่วนที่ไม่ได้แก้:** การอ่าน Excel, การแนบ PDF, ระบบกันแนบซ้ำ (resume) และขั้นตอนสร้างใบนำส่ง ยังเป็นโค้ดเดิมทั้งหมด

---

## 1. โครงสร้างใหม่

```
Main
 └─ CALL Flow3_Queue          ตัวจัดคิว (ใหม่)
      └─ CALL RunOneRound     ตัวทำงาน 1 ชุด (โค้ดเดิม + แก้ 5 จุด)
           └─ CALL PlayErrorAlarm   เสียงเตือน
```

| ไฟล์ | ใส่ในแท็บ (subflow) | จำนวนบรรทัด |
|---|---|---|
| `Flow3_Queue.txt` | `Flow3_Queue` | 282 |
| `RunOneRound.txt` | `RunOneRound` | 1,339 |
| `PlayErrorAlarm.txt` | `PlayErrorAlarm` | 27 |
| — | `Main` | 1 บรรทัด: `CALL Flow3_Queue` |

---

## 2. Flow3_Queue กับ RunOneRound ต่างกันอย่างไร

| | Flow3_Queue | RunOneRound |
|---|---|---|
| หน้าที่ | จัดการไฟล์และลำดับงาน | ทำงานบนเว็บ กยศ. |
| ทำงานกับ | โฟลเดอร์ `Queue\`, `Input\`, `ProcDocs\` | Excel, Chrome, เว็บ กยศ. |
| ขอบเขต | ทุกชุดในคิว | 1 ชุดต่อการเรียก 1 ครั้ง |
| รู้เรื่องเว็บไหม | ไม่รู้ | รู้ |
| รู้ว่ามีกี่ชุดในคิวไหม | รู้ | ไม่รู้ |

### ทำไมต้องแยกเป็น 2 ไฟล์

**1. คำสั่ง `EXIT` ของ Power Automate Desktop ปิดทั้งโปรแกรม**

โค้ดเดิมมี `EXIT` 11 จุด ถ้าเขียนลูปคิวไว้ในไฟล์เดียวกัน เจอ `EXIT` ครั้งเดียวลูปจะหยุดทั้งหมด การแยกให้ RunOneRound เป็น subflow ทำให้เราเปลี่ยน `EXIT` เป็นการกระโดดไปท้าย subflow ได้ พอ subflow จบ ระบบจะกลับไปทำต่อที่ Flow3_Queue เอง

**2. ดู error ง่าย**

เมื่อ PAD แจ้ง error จะบอกชื่อ subflow กับเลขบรรทัด เช่น `Subflow: Flow3_Queue, Line: 130` จึงรู้ทันทีว่าปัญหาอยู่ที่ส่วนจัดไฟล์หรือส่วนทำงานบนเว็บ ถ้ารวมเป็นไฟล์เดียวจะต้องไล่หาในโค้ดกว่า 1,600 บรรทัด

**3. แก้ส่วนหนึ่งไม่กระทบอีกส่วน**

ถ้าเว็บ กยศ. เปลี่ยนหน้าตา แก้เฉพาะ RunOneRound ถ้าจะเปลี่ยนวิธีจัดโฟลเดอร์ แก้เฉพาะ Flow3_Queue

> ตัวแปรใน PAD ใช้ร่วมกันทั้ง Flow การแยก subflow จึงไม่ได้ช่วยแยกตัวแปร เหตุผลหลักคือข้อ 1

---

## 3. Flow3_Queue — ตัวจัดคิว (ใหม่ทั้งไฟล์)

### โครงสร้างโฟลเดอร์คิว

```
Queue\
├─ 133-1.xlsx       ← ไฟล์ Excel ของชุด
├─ 133-1\           ← โฟลเดอร์ PDF ชื่อเดียวกัน
├─ 133-2.xlsx
├─ 133-2\
├─ _Working\        ← ชุดที่กำลังทำ (ต้องมีไม่เกิน 1 ชุด)
└─ Archive\         ← ชุดที่ทำเสร็จแล้ว
   └─ 20260913_143022_133-1\
```

### 3.1 เตรียมโฟลเดอร์ — บรรทัด 29–44

**ทำอะไร:** สร้าง `Queue\Archive\`, `Queue\_Working\`, `Log_Queue\`, `KTB\` ถ้ายังไม่มี

**ทำไม:** เครื่องใหม่ไม่ต้องสร้างเอง ถ้ามีอยู่แล้ว ระบบข้ามไป

### 3.2 กู้งานค้าง — บรรทัด 58–90

**ทำอะไร:** ถ้า `_Working\` มีไฟล์ค้าง แปลว่ารอบก่อนหยุดกลางคัน ระบบจะ

1. ย้ายไฟล์จาก `_Working\` กลับเข้า `Queue\`
2. ลบ Excel สำเนาที่ค้างใน `Input\`
3. เขียน `[RECOVERED]` ลง `queue.log`

**ทำไม:** เดิมต้องย้ายไฟล์กลับเองก่อนรันใหม่ ตอนนี้กด Run ได้ทันที ส่วนรายการที่แนบสำเร็จไปแล้ว ระบบกันแนบซ้ำเดิม (`resume_success.txt`) จะข้ามให้

**ไม่วนซ้ำไม่รู้จบ:** ถ้าปัญหาเดิมยังอยู่ ชุดนั้นจะล้มเหลวอีกครั้งแล้วระบบหยุด คือ 1 ครั้งต่อการกด Run 1 ครั้ง

### 3.3 ลูปหลัก 11 ขั้นต่อ 1 ชุด — บรรทัด 92–239

| ขั้น | บรรทัด | ทำอะไร | ทำไม |
|---|---|---|---|
| 1 | 94 | เช็คไฟล์ `State\QUEUE_STOP.txt` | ให้คนสั่งหยุดคิวได้ |
| 2 | 106 | หยิบ `.xlsx` ตัวแรกใน `Queue\` (เรียงตามชื่อ) | ไม่มีไฟล์เหลือ = คิวหมด จบการทำงาน |
| 3 | 125 | เช็คว่ามีโฟลเดอร์ PDF ชื่อเดียวกัน, มี PDF, ไม่เกิน 50 ไฟล์ | จับปัญหาก่อนย้ายไฟล์ใด ๆ |
| 4 | 145 | เช็คว่า `Input\` ว่าง | กันงาน 2 ชุดปนกัน |
| 5 | 153 | เช็คว่า `ProcDocs\` มีโฟลเดอร์ปี 1 อัน และเทอม 1 อัน | รู้ว่าต้องวาง PDF ไว้ที่ไหน |
| 6 | 169 | ย้ายไฟล์ชุดนี้เข้า `_Working\` | ถ้าไฟดับ รู้ว่าชุดไหนทำค้างไว้ |
| 7 | 173 | ล้าง `Processing\`, `Log\` และ**ทุกชุด**ใน `ProcDocs\ปี\เทอม\` | RunOneRound ทำงานกับทุกโฟลเดอร์ในเทอม ถ้าชุดเก่าค้างจะถูกแนบซ้ำ |
| 8 | 195 | copy Excel ไป `Input\` และ copy โฟลเดอร์ PDF ไป `ProcDocs\` | ใช้ copy เพื่อให้ต้นฉบับอยู่ครบใน `_Working\` |
| 9 | 202 | `CALL RunOneRound` แล้วรอจนจบ | ทำงานบนเว็บ |
| 10 | 215 | เช็คว่า `Input\` ว่างหรือยัง | RunOneRound ย้าย Excel ออกเฉพาะตอนทำครบ ถ้ายังค้าง = มีปัญหา |
| 11 | 223 | ย้าย `_Working\` ไป `Archive\วันที่_เวลา_ชื่อชุด\` | เก็บงานที่เสร็จ แล้วกลับไปขั้น 1 |

**ทำไมตรวจผลจาก `Input\`:** RunOneRound มีกติกาเดิมอยู่แล้วว่าจะย้าย Excel ออกจาก `Input\` ก็ต่อเมื่อไม่มีรายการล้มเหลว ใช้กติกานี้ต่อจึงไม่ต้องแก้โค้ดเดิมเพิ่ม

### 3.4 เมื่อมีปัญหา — บรรทัด 241–280

1. เขียน `[QUEUE_HALT]` พร้อมสาเหตุลง `Log_Queue\queue.log`
2. ส่งอีเมลแจ้ง
3. แสดงหน้าต่างเตือน (ปิดเองใน 15 วินาที เพื่อไม่ให้บล็อกเสียงเตือน)
4. เล่นเสียงเตือนทุก 20 วินาที จนกว่าจะมีไฟล์ `State\QUEUE_STOP.txt`

**ทำไมใช้ `QUEUE_STOP.txt` ไม่ใช้ `STOP.txt` ตัวเดิม:** RunOneRound ลบ `STOP.txt` ทิ้งทุกครั้งที่เริ่มทำงาน (บรรทัด 13 ของโค้ดเดิม) ถ้าใช้ไฟล์เดียวกัน ตัวจัดคิวจะมองไม่เห็นคำสั่งหยุด

---

## 4. RunOneRound — แก้จากโค้ดเดิม 5 จุด

### 4.1 เปลี่ยน `EXIT` เป็น `GOTO EndOfRound`

**ตำแหน่ง:** บรรทัด 54, 70, 218, 252, 282, 312, 340, 379, 424, 655, 808 และ `LABEL EndOfRound` ที่บรรทัด 1314

**ปัญหาเดิม:** `EXIT` ปิดทั้งโปรแกรม ลูปคิวจึงหยุดตาม

**ทำงานอย่างไร:** เจอเงื่อนไขที่ต้องหยุด ระบบกระโดดไปบรรทัดสุดท้ายของ RunOneRound แล้วกลับไปทำต่อที่ Flow3_Queue ขั้น 10

**ทำไมเลือกวิธีนี้:** แก้แค่ 11 บรรทัด ข้อความเตือน อีเมล และเสียงเตือนเดิมก่อนหน้า `EXIT` ยังทำงานเหมือนเดิม

### 4.2 อ่านเลข KTB ก่อนกดพิมพ์ (เลขสำรอง)

**ตำแหน่ง:** บรรทัด 1059–1070

**ทำอะไร:** ก่อนกด "ยืนยันจัดพิมพ์ใบนำส่ง" รัน JavaScript ค้นหาข้อความรูปแบบ `KTB-ตัวเลข-ตัวเลข` บนหน้าเว็บ แล้วเก็บไว้

**ทำไม:** เผื่อกรณีอ่านหลังพิมพ์ไม่สำเร็จ

### 4.3 รอแท็บ PDF หลายรอบ

**ตำแหน่ง:** บรรทัด 1108–1128

**ปัญหาเดิม:** รอ 5 วินาทีแล้วเช็คครั้งเดียว PDF 50 รายการสร้างนานกว่านั้น จึงเจอ error `ไม่ได้อยู่บนแท็บ PDF`

**ทำงานอย่างไร:**

```
วนสูงสุด 20 รอบ
  กด Ctrl+L, Ctrl+C เพื่อ copy URL ของแท็บที่เปิดอยู่
  URL ขึ้นต้นด้วย "blob:" ?
    ใช่ → เจอแท็บ PDF ออกจากลูป
    ไม่ → บันทึกลง print_tab_debug.log รอ 3 วินาที แล้วลองใหม่
```

**ทำไมเลือกวิธีนี้:** ใช้วิธีเช็คแบบเดิม (อ่าน URL) ที่ใช้งานได้อยู่แล้ว เพิ่มแค่การวนซ้ำ เจอเร็วก็ไปต่อเร็ว ไม่ต้องรอครบ 80 วินาที

### 4.4 บันทึกเลข KTB ลง `KTB\KTB.log`

**ตำแหน่ง:** บรรทัด 1131–1172

**ตัวอย่างผลลัพธ์:**

```
1. KTB-2569002053100211-001 ชุดที่ 133-1
2. KTB-2569002053100213-001 ชุดที่ 133-2
```

**ทำงานอย่างไร:**

1. ปิดแท็บ PDF กลับมาที่หน้าสรุป
2. บันทึกว่าพิมพ์สำเร็จลง `print_success.txt` **ก่อน**
3. รัน JavaScript อ่านข้อความทั้งหน้า แล้วดึง**ทุกข้อความ**ที่ตรงรูปแบบ `KTB-ตัวเลข-ตัวเลข` ลองได้สูงสุด 10 ครั้ง ครั้งละ 1 วินาที
4. ถ้าอ่านได้ ใช้เลขนี้ ถ้าไม่ได้ ใช้เลขสำรองจากข้อ 4.2
5. นับจำนวนบรรทัดใน `KTB.log` แล้วบวก 1 เป็นเลขลำดับ
6. เขียนลงไฟล์ ถ้าเขียนไม่สำเร็จ บันทึกลง `print_tab_debug.log` แทน

**เลข KTB มาจากไหน:** copy จากข้อความจริงบนหน้าเว็บ ช่อง "เลขที่ใบนำส่งเอกสาร" ระบบไม่ได้สร้างหรือเดาเลขเอง ถ้าหาไม่เจอจะเขียนว่า `NOT_FOUND`

**ทำไมเลือกวิธีนี้:**

| การตัดสินใจ | เหตุผล |
|---|---|
| อ่านจากหน้าเว็บ ไม่อ่านจาก PDF | ข้อความใน PDF ถูกบีบอัด ต้องมีตัวอ่าน PDF เพิ่ม ส่วนหน้าเว็บแสดงเลขเดียวกันเป็นข้อความธรรมดา |
| อ่านหลังพิมพ์เป็นหลัก | เลข KTB อาจสร้างหลังกดยืนยัน อ่านก่อนอาจยังไม่มีเลข |
| เก็บทุกเลขที่เจอ ไม่เลือกตัวแรก | ถ้าหน้ามีหลายเลข จะเห็นใน log ทันที ไม่บันทึกเลขผิดโดยไม่มีใครรู้ |
| บันทึกพิมพ์สำเร็จก่อนบันทึก KTB | ถ้าส่วน KTB มีปัญหา งานพิมพ์ยังนับว่าสำเร็จ ไม่พิมพ์ซ้ำ |
| นับเลขลำดับจากไฟล์ | ปิดโปรแกรมแล้วเปิดใหม่ เลขยังต่อกัน และรัน RunOneRound แยกได้ |

### 4.5 หยุดกลางคันแล้วปิด Chrome และ Excel

**ตำแหน่ง:** บรรทัด 1234–1243

**ปัญหาเดิม:** เมื่อสั่งหยุดหรือพิมพ์ล้มเหลว ระบบเปิด Chrome และ Excel ค้างไว้ Excel ที่ค้างล็อกไฟล์ใน `Processing\` ทำให้รอบถัดไป copy ไฟล์ทับไม่ได้

**ทำงานอย่างไร:** ปิด Chrome และ Excel เสมอ แต่ยังไม่ logout ไม่ส่งอีเมลสรุป และไม่ย้ายไฟล์ เพราะงานยังไม่เสร็จ

**ทำไม:** ข้อมูลสำหรับตรวจสอบมีครบใน log แล้ว ไม่จำเป็นต้องเปิดโปรแกรมค้างไว้

---

## 5. PlayErrorAlarm — แก้ path ไฟล์เสียง

| | path |
|---|---|
| เดิม | `Sound\Sounderror_alarm.wav` (คำว่า Sound ซ้ำ หาไฟล์ไม่เจอ) |
| ใหม่ | `Sound\error_alarm.wav` |

ย้ายออกจากท้ายโค้ดเดิมมาเป็นไฟล์แยก เพื่อให้ Flow3_Queue และ RunOneRound ใช้ตัวเดียวกัน

---

## 6. ไฟล์และโฟลเดอร์ที่เกิดขึ้นใหม่

| ที่อยู่ | สร้างโดย | ใช้ทำอะไร |
|---|---|---|
| `Queue\` | คน | วางงานรอคิว |
| `Queue\_Working\` | Flow3_Queue | ชุดที่กำลังทำ |
| `Queue\Archive\` | Flow3_Queue | ชุดที่ทำเสร็จ |
| `Log_Queue\queue.log` | Flow3_Queue | ประวัติการทำงานของคิว |
| `KTB\KTB.log` | RunOneRound | เลข KTB ของใบนำส่งแต่ละชุด |
| `State\QUEUE_STOP.txt` | คน | สั่งหยุดคิวและหยุดเสียงเตือน |

---

## 7. ข้อควรรู้

- **ลำดับการหยิบงานเรียงแบบตัวอักษร:** `133-1`, `133-10`, `133-2` ถ้าลำดับสำคัญ ให้ตั้งชื่อ `133-01`, `133-02`
- **ห้ามลบ `State\resume_success.txt`** เพราะเป็นระบบกันแนบซ้ำ
- **ปิด Excel ก่อนวางไฟล์เข้าคิว** ไม่อย่างนั้นไฟล์ล็อก `~$` จะถูกหยิบเป็นงาน
- รายละเอียดการติดตั้งดูที่ `SETUP.md`
